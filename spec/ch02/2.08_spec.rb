require "ch02/2.08.rb"

describe "2.8 Loop Detection" do
  let(:one) { LinkedList.new([1,2,3]) }
  let(:two) { LinkedList.new([4,5,6]) }
  let(:thr) { LinkedList.new([9,9,9]) }

  it "detects circular loop"

  it ""

end
